---
name: evidence-debate
description: 对抗式循证辩论工作流——从证据穷尽、全文获取、迭代攻防到证据分级、多版本交付。适用于评估技术路线的临床疗效可达性、科学尽职调查、投资决策支持。核心特点：穷尽可靠文献、全文级证据优先、40 轮迭代攻防、GRADE 证据分级、可证伪条件输出。
triggers: 对抗式辩论、循证辩论、疗效可达性评估、科学尽职调查、证据穷尽、全文获取、迭代攻防、GRADE 分级、可证伪条件、多版本交付
tools: Read, Write, Edit, Bash, Grep, Glob, TaskCreate, TaskUpdate, Agent
model: inherit
---

# Evidence Debate Skill — 对抗式循证辩论工作流

> 版本：v1.0 | 创建日期：2026-08-31
> 来源项目：761TEL 两条 NSC 管线临床疗效可达性评估（40 轮攻防、180 条候选、90 篇全文级）
> 核心原则：**穷尽可靠文献、全文级证据优先、迭代攻防、证据分级、可证伪条件、多版本交付**

---

## 何时使用

- 评估某条技术路线的**临床疗效可达性**（如"这条路能不能做出注册终点"）
- **科学尽职调查**（投资决策、技术引进、合作评估）
- 需要**对抗式审视**某个科学主张（避免单方面乐观或悲观）
- 需要**穷尽证据**后做出**可证伪**的结论
- 需要**多版本交付**（完整演化版 vs 精简结论版）

---

## 核心原则

### 1. 穷尽可靠文献

**不是"找几篇代表性的"，而是"穷尽所有相关的"。**

- 用 PubMed E-utilities 按多个维度全量检索（不是关键词搜索，是维度分解）
- 每个维度独立检索，然后合并去重
- 目标：**不漏掉任何一篇可能改变结论的文献**

### 2. 全文级证据优先

**能下载全文的，必须下载全文；不能下载全文的，只能用摘要中明确说明的数据，禁止延伸推理。**

证据分级：
- **全文级**：可下载全文 → 引用全文中的数据（一手）
- **摘要级**：不能下载全文但重要 → **仅引用摘要中明确说明的数据**（如"N=47"、"p=0.06"、"180 天 mRS 无显著改善"），**禁止根据摘要进行延伸、推理、想象后引用**
- **注册库**：ClinicalTrials.gov resultsSection 原始数据
- **排除**：无法获取全文且摘要无明确数据的文献

### 3. 迭代攻防

**不是"立论→质疑→反驳"一次完成，而是"每轮反方主攻→正方反驳→新浮现争点→判决"，直到收敛。**

- 每轮必须产生**新浮现争点**（从上轮的反驳中长出，不是预设的）
- 争点必须**逐步深入**（从表面问题到本质问题）
- 判决必须**可证伪**（给出明确的检验条件）

### 4. 证据分级（GRADE）

**每条结论都要标注证据确定性等级，不是"有效/无效"的二元判断。**

- 五个降级因素：偏倚风险、不一致性、间接性、不精确性、发表偏倚
- 三个升级因素：大效应量、剂量-反应关系、残余混杂方向有利
- 输出格式：**等级（高/中/低/极低）+ 达标概率区间**

### 5. 可证伪条件

**每个判决都要给出"什么情况下这个判决会被推翻"的明确条件。**

- 不是"如果有效就好"，而是"如果 IIT 中 N 例患者 90 天 mRS ≤2 比例 <X%，则路线不成立"
- 让判决**可以被检验**，而不是永远正确

### 6. 多版本交付

（见阶段 6）

### 7. 引用回溯核查（交付前的最后关口）

**攻防完成、判决形成之后、交付之前，必须对正文实际引用的每一条做逐篇精读与核查。**

依据是一次真实事故：某项目经 40 轮攻防形成的判决，在逐篇精读核查后发现
**17 篇引用无一篇完全准确**，其中两条支撑最终判决的核心数据**在原始文献中根本不存在**。
核查后两条管线的可达概率各下调 5–10 个百分点，资源分配建议随之改变。

四类必查错误：

| 类型 | 说明 |
|---|---|
| ① 数据不存在 | 含**跨文献归属错误**（把 A 文献的数字记成 B 的）——最常见 |
| ② 跨外推未标注 | 物种 / 疾病模型 vs 假手术模型 / 给药途径 / 适应症 |
| ③ 统计量或时期错配 | WMD 当组内变化、SMD 当量表分值、急性期数据当慢性期 |
| ④ 原文限定语被删除 | `not solely A but also B` → 不依赖 A；`may shift` → 会转换 |

完整规范见 `references/citation_audit.md`；逐篇精读模板见 `references/paper_reading_protocol.md`。

> **处置原则**：凡经核查证伪的表述，无论其结论对哪一方有利，一律更正并在正文保留修订痕迹；
> 撤回关键论据后**必须重跑攻防**，不能只做局部修补。
>
> **v3 扩展**：① 核查范围可升级为**全库精读**（见阶段 3.6）——只精读引用文献无法发现引用错误的文献本身有问题；
> ② 核查后必须执行**修正后最终攻防**（见阶段 3.7）——从发现新证据转向最终整合与收敛；
> ③ 必查错误已扩充为**五类**（新增：引语不存在、负面证据遗漏与类别级过度概括），见 。

**完整演化版（内部决策）+ 精简结论版（对外汇报）。**

- 完整版：保留全部攻防过程、中间判决值、演化路径
- 精简版：仅保留最终结论、核心证据、可证伪条件

---

## 工作流（六阶段 + 三阶段核查深化）

> 阶段 0–6 为完整流水线；阶段 3.5（引用回溯核查）、3.6（全库精读）、3.7（修正后最终攻防）
> 是 v2/v3 实战中沉淀的**强制或强烈推荐**的深化阶段，插在阶段 3（迭代攻防）与阶段 4（证据分级）之间。

### 阶段 0：任务分类与澄清

**判断任务类型，确定走哪条线。**

| 用户说的 | → 走哪条线 |
|---------|-----------|
| 「评估 XX 的疗效可达性」「XX 能不能做出注册终点」 | **A 线**（深度调研 + 复杂推理辩论） |
| 「帮我做科学尽职调查」「评估 XX 技术路线」 | **A 线** |
| 「为什么/怎么做/选哪个」 | **B 线**（复杂推理） |
| 「帮我写/润色/改写」 | **C 线**（写作产出） |

**澄清关键问题**（≤3 个，用 AskUserQuestion）：
1. **疗效判据**：注册终点可达成（统计学显著 + ≥MCID）vs 机制成立 vs 分层双判据
2. **证据标准**：全文级优先 vs 摘要级可用 vs 严格全文级
3. **篇幅深度**：深度版（4 轮攻防）vs 极限版（10+ 轮攻防）

### 阶段 1：证据穷尽

**目标：找到所有可能改变结论的文献，不漏掉任何一篇。**

#### 1.1 维度分解

把研究问题分解为**多个独立的检索维度**（不是关键词组合，是维度拆分）：

| 维度类型 | 示例（以"NSC 治疗卒中"为例） |
|---|---|
| **给药途径** | IA 给药 NSC 卒中、脑内移植 NSC 卒中 |
| **时间窗** | 亚急性期、慢性期 |
| **机制** | 细胞替代、旁分泌、归巢、突触整合 |
| **对照** | 安慰效应、假手术对照、自然恢复 |
| **影像学** | DTI/CST 完整性、预后预测 |
| **替代方案** | 外泌体、其他细胞类型 |
| **系统综述** | 荟萃分析、Cochrane 综述 |

#### 1.2 全量检索

用 PubMed E-utilities 按每个维度**全量检索**（不是 top 10，是全部命中）：

```bash
EUTILS="~/.workbuddy/skills/ms-search-lit/references/pubmed_eutils.sh"
PARSER="~/.workbuddy/skills/ms-search-lit/references/parse_pubmed.py"
PY="C:/Users/G1381/.workbuddy/binaries/python/versions/3.13.12/python.exe"

# 每个维度独立检索
for Q in "intra-arterial neural stem cell stroke" "intracerebral neural stem cell chronic stroke transplant" "Fugl-Meyer chronic stroke sham placebo"; do
  IDs=$(bash "$EUTILS" search "$Q" 50 | "$PY" -c "import sys,json;print(','.join(json.load(sys.stdin)['esearchresult']['idlist']))")
  echo "维度: $Q | 命中: $(echo $IDs | tr ',' ' ' | wc -w) 条"
  bash "$EUTILS" fetch_json "$IDs" | "$PY" "$PARSER" esummary > "${Q// /_}_list.txt"
done
```

#### 1.3 合并去重

把所有维度的结果合并，按 PMID 去重：

```python
import json, glob

all_records = {}
for f in glob.glob('*_list.txt'):
    dim = f.replace('_list.txt','')
    for line in open(f, encoding='utf-8'):
        parts = line.strip().split('|')
        if len(parts) >= 4:
            pmid = parts[0]
            if pmid not in all_records:
                all_records[pmid] = {
                    'pmid': pmid, 'year': parts[1], 'journal': parts[2],
                    'title': parts[3], 'dimensions': [dim]
                }
            else:
                all_records[pmid]['dimensions'].append(dim)

print(f'去重后总数: {len(all_records)}')
```

**目标：180 条候选**（参考值，根据领域调整）。

### 阶段 2：全文获取与证据分级

**目标：下载所有可获全文的文献，按证据等级分级。**

#### 2.1 全文可得性判定

用 Europe PMC 判定每篇文献的全文可得性：

```python
import json, time, urllib.request

def check_epmc(pmid):
    url = f'https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=EXT_ID:{pmid}&resultType=core&format=json'
    try:
        req = urllib.request.Request(url, headers={'User-Agent':'Mozilla/5.0'})
        with urllib.request.urlopen(req, timeout=30) as r:
            d = json.loads(r.read().decode())
        res = (d.get('resultList') or {}).get('result') or []
        if res:
            r0 = res[0]
            return {
                'pmcid': r0.get('pmcid'),
                'inEPMC': r0.get('inEPMC'),
                'isOpenAccess': r0.get('isOpenAccess'),
                'hasFullText': bool(r0.get('pmcid') and r0.get('inEPMC') == 'Y' and r0.get('isOpenAccess') == 'Y')
            }
    except:
        pass
    return {'pmcid': None, 'inEPMC': None, 'isOpenAccess': None, 'hasFullText': False}

# 批量处理
records = json.load(open('all_records.json', encoding='utf-8'))
for r in records:
    info = check_epmc(r['pmid'])
    r.update(info)
    time.sleep(0.35)

fulltext = [r for r in records if r['hasFullText']]
abstract_only = [r for r in records if not r['hasFullText']]
print(f'全文级: {len(fulltext)}, 摘要级: {len(abstract_only)}')
```

**目标：90 篇全文级**（参考值，根据领域调整）。

#### 2.2 批量下载与解析

用 `ms-fulltext-retrieval` 的 `epmc_search.py` 批量下载并解析：

```bash
S="~/.workbuddy/skills/ms-fulltext-retrieval"
PY="C:/Users/G1381/.workbuddy/binaries/python/versions/3.13.12/python.exe"

# 提取全文级 PMCID
PMCIDS=$(python -c "import json; records=json.load(open('all_records_graded.json')); print(' '.join([r['pmcid'] for r in records if r['hasFullText']]))")

# 批量下载
"$PY" "$S/references/epmc_search.py" fetch $PMCIDS -o pdfs/

# 批量解析
for P in $(ls pdfs/*.xml | sed 's/.xml//'); do
  "$PY" "$S/references/jats_to_text.py" $P.xml > texts/$P.txt
done
```

#### 2.3 注册库数据获取

用 `ctr_fetch.py` 获取临床试验注册库的原始数据：

```bash
"$PY" "$S/references/ctr_fetch.py" NCT02448641 --all
```

**输出**：N、分期、状态、结果上传日、各组计数、自动算百分比。

#### 2.4 证据分级标注

每条引用标注获取状态：
- **`[全文]`**：Europe PMC JATS 全文，已通读
- **`[摘要级]`**：付费墙文献，仅引用摘要中明确说明的数据，禁止延伸推理
- **`[注册库]`**：ClinicalTrials.gov resultsSection 原始数据
- **`[内部文件]`**：企业内部文件

### 阶段 3：迭代攻防

**目标：通过多轮对抗式辩论，逐步深入到本质问题，直到收敛。**

#### 3.1 攻防结构

每轮攻防的结构：

```
反方主攻（质疑）→ 正方反驳 → 再质疑（反方）→ 新浮现争点 → 判决
```

**关键规则**：
- 每轮必须产生**新浮现争点**（从上轮的反驳中长出，不是预设的）
- 争点必须**逐步深入**（从表面问题到本质问题）
- 判决必须**可证伪**（给出明确的检验条件）

#### 3.2 攻防轮次设计

**参考：40 轮攻防**（根据问题复杂度调整）：

| 阶段 | 轮次 | 目标 |
|---|---|---|
| **R1–R4** | 初步攻防 | 识别表面问题（递送效率、安全性、初步疗效） |
| **R5–R10** | 深化攻防 | 深入到机制层面（细胞替代 vs 旁分泌、体内分化、剂量-效应） |
| **R11–R20** | 重新定位 | 根据新证据重新评估（超选给药、时间窗效应、影像学分层） |
| **R21–R30** | 穷尽证据 | 基于全部文献重新评估（时间窗梯度、资源分配、核心叙事） |
| **R31–R40** | 收敛 | 最终判决与行动建议（可证伪条件、资源分配、多版本交付） |

#### 3.3 争点演化示例

**管线一（IA 给药 NSC × 亚急性卒中）**：

| 轮 | 反方主攻 | 正方回应 | 新浮现争点 |
|---|---|---|---|
| R1 | IA 入脑率 0.6–0.9%，40–47% 归肝脾 | 亚急性期 BBB 开放 + 趋化梯度存在 | → 入脑率是否足以支撑何种机制？ |
| R2 | 剂量-效应平坦而毒性陡峭（Fukuda/Cui） | 研究对象为 BMSC，NSC 体积更小 | → **若剂量受限，起效机制只能是旁分泌** |
| R3 | 若机制是旁分泌，端脑特异性失效 | NSC 分泌组含神经特异性营养因子 | → 那与 MSC/外泌体相比优势何在？ |
| R4 | 荟萃的类别效应属 MSC，非 NSC | 长期终点（365 天）信号稳健 | → **管线三是否比管线一更自洽** |
| R7 | — | — | **体外 84% ≠ 体内 0.015%**，体外数据不可用 |
| R11 | — | — | **超选给药**改变了剂量-暴露关系，需重新检验 |
| R21 | — | — | **时间窗效应**：慢性期可能优于亚急性期 |

**演化结论**：争点从最初的"递送效率"逐步收敛为"**机制定位决定一切**"——机制主张决定了所需剂量，剂量决定了给药途径的可行性，而给药途径又反过来限制机制主张。

#### 3.4 判决输出

每轮的判决必须包含：

| 项目 | 内容 |
|---|---|
| **可达概率** | 注册终点可达概率区间（如 15–25%） |
| **GRADE 等级** | 证据确定性（高/中/低/极低） |
| **降级因素** | 触发的 GRADE 降级因素（偏倚风险、不一致性、间接性、不精确性、发表偏倚） |
| **升级因素** | 触发的 GRADE 升级因素（大效应量、剂量-反应、残余混杂有利） |
| **可证伪条件** | 明确的检验条件（如"若 IIT 中 N 例患者 90 天 mRS ≤2 比例 <X%，则路线不成立"） |

### 阶段 3.5：引用回溯核查（强制，不可跳过）

对**正文实际引用的每一条**（不是全部候选文献）逐篇精读并核查。

**执行方式**：多个 Agent 并行，每 Agent 负责 2–3 篇，每篇输出独立精读文件（八节结构）。

**输入准备**：

1. 提取正文中每一处引用的上下文（含引用的完整句子）→ 存为 JSON，作为 Agent 的核查靶子
2. 提取全文文本（**必须含表格**——注意表格可能位于 `<floats-group>` 而非 `<body>`）

**Agent 提示词必须包含**：

- 待核查文献的 PMCID / 标题
- 报告中引用该文献的**逐条上下文**
- 明确要求：**完整通读全文后再动笔，禁止只读 grep 片段**
- 明确要求：主动报告「原文没说什么」（负面清单）
- 明确要求：不得遗漏对己方不利的证据

**产出**：每篇一个精读文件，第 7 节逐条给出判定（准确 / 部分偏差 / 错误），
并贴出原文英文原句作为依据。

**后续**：若有「错误」级判定被确认 → 撤回相关结论 → **重跑一轮攻防** → 更新判决与资源分配。

> **⚠️ v3 升级：五类必查错误**（详见 `references/citation_audit.md`）：
> ① 数据不存在（含跨文献归属）｜①b **引语不存在**（最隐蔽——每个带引号的英文引语必须 grep 全文逐字确认）
> ② 跨外推未标注（物种/疾病模型 vs 假手术/途径/适应症）
> ③ 统计量或时期错配（WMD 当组内变化、SMD 当分值、登记数当分析数、随访时长当时间窗）
> ④ 原文限定语被删除（`not solely`、`may`、`no evidence of`）
> ⑤ **负面证据遗漏与"类别级"过度概括**（如"外泌体机制自洽"实为特定条件下的自洽，外泌体有双向性）
>
> 修正原则：**证伪即改，无论对哪一方有利**；修正不选择方向。

---

### 阶段 3.6：全库精读（可选但强烈推荐）

对**全部已下载全文**（而非仅正文引用）执行精读。规范见 `references/full_library_reading.md`。

**为什么必要**：真实教训——只精读引用文献时，106 篇中 84%（89 篇）从未进入论证，
而其中包含改变论证结构的证据（如"IA 后细胞不入脑"的四条独立来源有三条来自未精读文献）。

**触发条件**：用户明确要求；或证据库 ≤150 篇且已全量下载全文时建议直接全库精读。

**执行要点**：
1. 批量提取全文（`extract_paper.py`，**必须能提取 `<floats-group>` 中的表格**）
2. 按主题分批，6 Agent 并行 × 3–4 篇；**低相关文献单独成批**（心脏/兽医/材料等）
3. 对未引用文献：判断"是否含有足以改变判决的关键数据" + 建立**防误引负面清单**（"这篇不能用于什么"）
4. 用脚本扫描全部精读文件第 7 节，提取修正清单（几百条问题中只有影响正文的才需改报告）
5. 修正并保留修订痕迹；更新"关键发现汇总"（按证据强度排序，标注对判决的影响）

**全库精读的常见坑**：只精读引用文献、表格遗漏（floats-group）、Agent 只读 grep 片段、
低相关文献跳过、修正选择方向、引语只核方向不核逐字。

---

### 阶段 3.7：修正后最终攻防（v3 新增）

引用修正完成后，**必须再推一轮攻防**（R51–R60 式），从"发现新证据"转向"最终整合"：

**本阶段的三个任务**：
1. **确认修正后的事实链**——把全部修正结果压缩为 8–12 条核心事实（F1–F12），每条带出处与限定；
2. **检验判决在修正后是否仍成立**——对每条判决问"修正改变了它吗？改变了哪部分？"；
3. **输出最终收敛**——含三条管线的"未决问题清单"（每个未决问题对应一个可证伪条件）。

**最终收敛的结构**（参考 `05_辩论轮次/R51-R60_最终攻防.md` 与 `05_辩论轮次/R51-R60_最终攻防.md`）：

- **R51 式**：对最核心事实（如"IA 细胞不入脑"）做定性升级（从"量少"到"不进入"）
- **R53 式**：从交锋中长出**全新争点**（如"超选导管与保持血流的潜在冲突"）
- **R58 式**：跨线整合——把各管线表述为"不同性质的赌注"（技术突破 / 设计改进 / 产品定义）
- **R60 式**：最终判决 + 资源分配 + 可证伪条件 + 最终声明（"不回答是否值得投资，只回答证据能否支撑终点可达"）

**最终声明模板**：

> 本报告不回答"是否值得投资"——那是商业判断；只回答"证据能否支撑注册终点可达"。
> 结论：各管线在现有证据下均不构成"可达"的充分条件，但均未被完全排除，
> 其命运取决于各自的未决问题能否被正面回答。

---

### 阶段 4：证据分级与跨层一致性检验

**目标：用 GRADE 体系评估每条结论的证据确定性，用三层证据检验跨层一致性。**

#### 4.1 GRADE 证据分级

对每条关键结论，评估五个降级因素与三个升级因素：

| 降级因素 | 检查项 | 示例 |
|---|---|---|
| **偏倚风险** | 研究设计是否存在偏倚？ | 开放标签单臂、无假手术对照 |
| **不一致性** | 不同研究结果是否一致？ | 单臂 +16 分 vs 对照试验失败 |
| **间接性** | 证据是否直接适用于目标问题？ | 动物→人、BMMNC→NSC、静脉→动脉 |
| **不精确性** | 样本量是否足够？置信区间是否过宽？ | N=9、CI 下限 1.01 |
| **发表偏倚** | 是否存在发表偏倚？ | ACTIsSIMA 2018 完成、2020 上传结果、至今未发表 |

| 升级因素 | 检查项 | 示例 |
|---|---|---|
| **大效应量** | 效应量是否足够大（RR > 2 或 < 0.5）？ | +16 分 vs 安慰 +2 分 |
| **剂量-反应** | 是否存在剂量-反应关系？ | 动物数据有，人体数据无 |
| **残余混杂有利** | 残余混杂是否方向有利？ | 开放标签引入的混杂方向不利 |

**输出格式**：`等级（高/中/低/极低）+ 可达概率区间`

#### 4.2 三层证据与跨层一致性

| 层 | 内容 | 角色 |
|---|---|---|
| **L1 基础** | 细胞生物学、分化机制、分泌组 | 判断机制 plausibility |
| **L2 临床前** | 动物模型疗效与安全性 | 判断剂量-效应与毒性边界 |
| **L3 临床** | RCT、注册库、荟萃 | 判断注册终点可达性 |

**跨层一致性判读**：
- **L1+L2 强而 L3 弱** = 典型转化失败模式
- **L1 弱而 L3 强** = 需高度警惕安慰效应
- **三层一致** = 高置信

### 阶段 5：可证伪条件与行动建议

**目标：给出明确的可证伪条件与行动建议，让判决可以被检验。**

#### 5.1 可证伪条件

每个判决都要给出"什么情况下这个判决会被推翻"的明确条件：

**管线一**：若 IIT 中，超选 IA 给药的 N 例患者，其 90 天 mRS 0–2 比例与历史对照的差异 <10 个百分点，或出现 ≥1 例症状性栓塞，则证实"超选 IA 的疗效优势不成立"，需重估路线。

**管线二**：若 761TEL 的 IIT 采用假手术对照且同时报告 FMMS 平均变化与应答率，出现"平均改善 ≥10 分但应答率与对照组无差异"，则证实单臂报告的改善由少数极端值驱动，该终点体系需废弃。

**管线三**：若 24 个月内无法确定 NSC 外泌体的关键效应 miRNA（效力表征），则管线三的 IND 申报需无限期推迟。

#### 5.2 行动建议

按优先级给出行动建议：

| 优先级 | 行动 | 目的 |
|---|---|---|
| **P0** | 管线二 IIT 设计加入假手术对照 + 影像学分层（中脑脚 FA 值）+ 双终点 + 年龄分层 | 避免重蹈 ACTIsSIMA 覆辙 |
| **P0** | 明确机制主张（旁分泌主导），据此调整叙事 | 解决自洽性缺口 |
| **P1** | 管线三确定效应 miRNA（效力表征） | IND 申报的核心前提 |
| **P1** | 管线一补充超选 IA 的体内药代数据 | 验证超选优势 |
| **P2** | 管线一 IIT 设计加入时间窗分层（15–30 天 vs 7–14 天） | 验证最佳时间窗 |
| **P2** | 融资叙事调整为"三管线分层驱动" | 匹配证据状态 |

### 阶段 6：多版本交付

**目标：产出完整演化版（内部决策）+ 精简结论版（对外汇报）。**

#### 6.1 完整演化版

**特点**：
- 保留全部攻防过程（R1–R40）
- 保留中间判决值（如 10–20% → 15–25% 的演化过程）
- 保留争点演化表（每轮的反方主攻、正方回应、新浮现争点）
- 适用场景：**内部决策、团队讨论、需要追溯"判决是如何演化的"**

**结构**：
```
## 执行摘要（最终判决）
## 0. 方法学声明
## 1. 管线一
   1.0 【一号争点】（R1–R4）
   1.1 【二号争点】（R1–R4）
   1.2 四轮攻防与争点演化（R1–R4）
   1.3 第 5–10 轮攻防（续）
   1.4 第 11–20 轮攻防（续）
   1.5 第 21–30 轮攻防（续）
   1.6 第 31–40 轮攻防（续）
## 2. 管线二（同结构）
## 3. 交叉辩论
## 4. 双线判决总表（最终版）
## 5. 既有报告乐观偏差修正清单
## 6. 三层证据矩阵
## 参考文献（正文引用 + 补充参考文献）
```

#### 6.2 精简结论版

**特点**：
- 仅保留最终结论（15–25% / 25–35%）
- 去掉中间轮的过程性判决
- 保留关键发现（R7/R11/R13/R21/R39 等改变判决走向的节点）
- 适用场景：**投资人沟通、对外汇报、只需要最终结论**

**结构**：
```
## 执行摘要（最终判决）
## 0. 方法学声明
## 1. 管线一
   1.0 【一号争点】（最终判决）
   1.1 【二号争点】（最终判决）
   1.2 第 5–40 轮攻防：关键发现与最终调整（仅保留 R7/R11/R13/R21/R39 等）
## 2. 管线二（同结构）
## 3. 交叉辩论
## 4. 双线判决总表（最终版）
## 5. 既有报告乐观偏差修正清单
## 6. 三层证据矩阵
## 参考文献（正文引用 + 补充参考文献）
```

#### 6.3 版本说明

在精简版头部明确标注：

> 本版仅保留 40 轮攻防后的**最终结论与核心证据**，中间轮的过程性判决已省略。完整演化版见同目录《完整版.md》。

---

## 依赖技能与工具

### 核心依赖

| 技能/工具 | 用途 | 路径 |
|---|---|---|
| **ms-search-lit** | PubMed E-utilities 全量检索 | `~/.workbuddy/skills/ms-search-lit/` |
| **ms-fulltext-retrieval** | Europe PMC 全文获取、JATS 解析、注册库取数 | `~/.workbuddy/skills/ms-fulltext-retrieval/` |
| **md2docx_diy** | Markdown → DOCX 转换（纵向 A4） | `~/.workbuddy/skills/md2docx_diy/` |
| **agent-review_diy** | 五维审查（事实性/完整性/逻辑一致性/格式合规/AI 味） | `~/.workbuddy/skills/agent-review_diy/` |
| **fable5-workflow_diy** | 工作流编排（阶段 0–6） | `~/.workbuddy/skills/fable5-workflow_diy/` |

### 关键脚本

| 脚本 | 用途 | 路径 |
|---|---|---|
| `pubmed_eutils.sh` | PubMed E-utilities 检索 | `~/.workbuddy/skills/ms-search-lit/references/pubmed_eutils.sh` |
| `parse_pubmed.py` | PubMed 结果解析 | `~/.workbuddy/skills/ms-search-lit/references/parse_pubmed.py` |
| `epmc_search.py` | Europe PMC 检索与全文下载 | `~/.workbuddy/skills/ms-fulltext-retrieval/references/epmc_search.py` |
| `jats_to_text.py` | JATS XML → 纯文本转换 | `~/.workbuddy/skills/ms-fulltext-retrieval/references/jats_to_text.py` |
| `ctr_fetch.py` | ClinicalTrials.gov 注册库取数 | `~/.workbuddy/skills/ms-fulltext-retrieval/references/ctr_fetch.py` |

### Python 环境

- **Python 3.13.12**（managed）：`C:\Users\G1381\.workbuddy\binaries\python\versions\3.13.12\python.exe`
- 依赖：`pymupdf`（已装）、`pymupdf4llm`（可选）

---

## 环境要求

### 目录结构

```
{workspace}/subproj-{任务名}/
├── 00_交付/                    # 最终交付物（MD + DOCX）
├── 01_全文_JATS/               # 下载的全文 XML
├── 02_解析文本/                # 解析后的纯文本
├── 03_注册库结果/              # ClinicalTrials.gov 注册库数据
├── 04_证据矩阵/                # 证据索引、候选清单、分级结果
├── 05_辩论轮次/                # 每轮攻防的独立文件
└── 06_日志/                    # 执行日志
```

### 网络要求

- **Europe PMC**：全文获取与检索（需访问 `www.ebi.ac.uk`）
- **ClinicalTrials.gov**：注册库数据（需访问 `clinicaltrials.gov`）
- **PubMed**：E-utilities 检索（需访问 `eutils.ncbi.nlm.nih.gov`）

### 磁盘空间

- 180 篇全文 XML：约 **20–30 MB**
- 108 篇解析文本：约 **5–10 MB**
- 总需求：**<100 MB**

---

## 关键经验与陷阱

### 1. 检索穷尽性原则

**不要"找几篇代表性的"，要"穷尽所有相关的"。**

- 用**维度分解**而不是关键词组合
- 每个维度独立检索，然后合并去重
- 目标：**不漏掉任何一篇可能改变结论的文献**

**陷阱**：只检索一个维度（如"IA 给药 NSC 卒中"），漏掉其他维度（如"时间窗效应"、"影像学分层"）。

### 2. 全文获取的证据分级

**能下载全文的，必须下载全文；不能下载全文的，只能用摘要中明确说明的数据，禁止延伸推理。**

- **全文级**：引用全文中的数据（一手）
- **摘要级**：仅引用摘要中明确说明的数据（如"N=47"、"p=0.06"），**禁止**根据摘要进行延伸、推理、想象后引用

**陷阱**：把摘要中的"无显著改善"延伸为"证明无效"——摘要只说"无显著改善"，未说"无效"。

### 3. 迭代攻防的争点演化

**每轮必须产生新浮现争点，从上轮的反驳中长出，不是预设的。**

- 争点必须**逐步深入**（从表面问题到本质问题）
- 判决必须**可证伪**（给出明确的检验条件）

**陷阱**：每轮重复同样的争点，没有深入——比如一直纠结"递送效率"，而不深入到"机制定位"。

### 4. GRADE 证据分级的降级与升级

**每条结论都要标注证据确定性等级，不是"有效/无效"的二元判断。**

- 五个降级因素：偏倚风险、不一致性、间接性、不精确性、发表偏倚
- 三个升级因素：大效应量、剂量-反应关系、残余混杂方向有利

**陷阱**：只看效应量大小（如 +16 分），不看证据等级（如 N=9、开放标签、无对照）——大效应量但低证据等级，结论仍然是"未证实"。

### 5. 可证伪条件的重要性

**每个判决都要给出"什么情况下这个判决会被推翻"的明确条件。**

- 不是"如果有效就好"，而是"如果 IIT 中 N 例患者 90 天 mRS ≤2 比例 <X%，则路线不成立"
- 让判决**可以被检验**，而不是永远正确

**陷阱**：给出"如果有效就好"的模糊条件——这无法检验，也无法指导下一步行动。

### 6. 多版本交付的必要性

**完整演化版（内部决策）+ 精简结论版（对外汇报）。**

- 完整版：保留全部攻防过程、中间判决值、演化路径
- 精简版：仅保留最终结论、核心证据、可证伪条件

**陷阱**：只出一个版本——如果给投资人看完整版，他们会看到"前后不一致"的中间值；如果给团队看精简版，他们无法理解"判决是如何演化的"。

### 7. 检索疏漏的防范

**用产品代号检索后，必须逐篇核对命中结果，不可因前几条是"其他适应症"就推断"该适应症无发表"。**

**案例**：初查 NSI-566 时因仅核对了前两条命中（均为脊髓适应症）而误判"卒中数据空白"，经用户指出后已核实并更正，卒中 I 期结果实为已发表。

**防范**：
- 检索式应补 NCT 号反查（本例 NCT 号反查为 0，须靠作者名+适应症定向检索）
- 用**作者名 + 适应症**定向检索，而不是仅依赖产品代号

### 8. 数据核对的必要性

**所有数据必须核对，不能凭印象写。**

**案例**：Fukuda 剂量比，初稿写"相差约 9 倍"，实际是 **91 倍**（36.4 / 0.4 = 91）。

**防范**：
- 所有数据在引用前必须核对原始文献
- 关键数据（如剂量比、效应量、样本量）必须双人核对或独立验证

### 9. 中间判决值与最终值的一致性

**中间轮的判决值与最终值必须标注清楚，避免"前后不一致"的困惑。**

**案例**：管线一可达概率在 1.1 节写 10–20%，摘要写 15–25%——这不是"哪个对"的问题，而是**演化过程中的中间值没有标注清楚**。

**防范**：
- 在中间值后补充"后于 R11/R30 上调至最终值"的说明
- 或在精简版中只保留最终值

### 10. 参考文献的完整性

**下载了的所有全文级文献，都必须在参考文献中体现，不能只用 31 条。**

**案例**：90 篇全文级文献已全部下载并解析，但仅 31 条在正文引用——其余 59 篇下载了却没在报告里体现。

**防范**：
- 将全部全文级文献纳入参考文献，按主题分组
- 正文引用的文献保留原有编号与关键数据标注
- 其余文献按主题归类列出，均标注 PMCID 与核心内容

---

## 输出物清单

| 文件 | 路径 | 说明 |
|---|---|---|
| **完整演化版** | `00_交付/{任务名}_完整版.md` | 保留全部攻防过程、中间判决值、演化路径 |
| **精简结论版** | `00_交付/{任务名}_精简版.md` | 仅保留最终结论、核心证据、可证伪条件 |
| **DOCX 完整版** | `00_交付/{任务名}_完整版.docx` | 纵向 A4，用于内部决策 |
| **DOCX 精简版** | `00_交付/{任务名}_精简版.docx` | 纵向 A4，用于对外汇报 |
| **证据索引** | `04_证据矩阵/证据索引_v1.md` | 180 条候选、90 篇全文级、87 篇摘要级 |
| **辩论轮次** | `05_辩论轮次/R{开始}-{结束}_攻防.md` | 每轮攻防的独立文件 |
| **注册库数据** | `03_注册库结果/{NCT号}.json` | ClinicalTrials.gov 原始数据 |

---

## 版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0.0 | 2026-08-31 | 初始版本，基于 761TEL 两条 NSC 管线临床疗效可达性评估任务（40 轮攻防、180 条候选、90 篇全文级）固化 |

---

## 使用示例

### 示例 1：评估某条技术路线的疗效可达性

```
用户输入：评估 761TEL 管线一（IA 给药 NSC × 亚急性卒中）的临床疗效可达性

执行流程：
1. 阶段 0：任务分类 → A 线（深度调研 + 复杂推理辩论）
2. 阶段 1：证据穷尽
   - 维度分解：IA 给药 NSC 卒中、亚急性期、剂量-效应、安全性、时间窗
   - 全量检索：每个维度 50 条，合并去重后 37 条
3. 阶段 2：全文获取与证据分级
   - 全文可得性判定：9 篇全文级 / 28 篇摘要级
   - 批量下载与解析：9 篇全文级
   - 注册库数据：NCT02448641（ACTIsSIMA）
4. 阶段 3：迭代攻防（40 轮）
   - R1–R4：识别表面问题（递送效率、安全性、初步疗效）
   - R5–R10：深入到机制层面（细胞替代 vs 旁分泌、体内分化、剂量-效应）
   - R11–R20：重新定位（超选给药、时间窗效应、影像学分层）
   - R21–R30：穷尽证据（时间窗梯度、资源分配、核心叙事）
   - R31–R40：收敛（最终判决与行动建议）
5. 阶段 4：证据分级与跨层一致性检验
   - GRADE 分级：低（降级：间接性、不精确性、不一致性）
   - 跨层一致性：L1+L2 强、L3 弱 = 典型转化失败模式
6. 阶段 5：可证伪条件与行动建议
   - 可证伪条件：若 IIT 中 N 例患者 90 天 mRS ≤2 比例 <10%，则路线不成立
   - 行动建议：明确机制主张（旁分泌主导），补充超选 IA 体内药代数据
7. 阶段 6：多版本交付
   - 完整演化版：42,414 字，128 条参考文献
   - 精简结论版：33,837 字，115 条参考文献
```

### 示例 2：科学尽职调查

```
用户输入：帮我做 761TEL 的科学尽职调查，评估两条管线的投资价值

执行流程：
1. 阶段 0：任务分类 → A 线
2. 阶段 1：证据穷尽
   - 维度分解：管线一（IA 给药 NSC × 亚急性）、管线二（脑内移植 NSC × 慢性）、管线三（外泌体）、对照（安慰效应、自然恢复）、影像学（DTI/CST）、系统综述
   - 全量检索：每个维度 50 条，合并去重后 180 条
3. 阶段 2：全文获取与证据分级
   - 全文可得性判定：90 篇全文级 / 87 篇摘要级 / 2 项注册库 / 1 篇排除
   - 批量下载与解析：106 篇 XML、108 篇解析文本
   - 注册库数据：NCT02448641（ACTIsSIMA）、NCT03296618（NSI-566）
4. 阶段 3：迭代攻防（40 轮）
   - 管线一：R1–R20（超选给药、剂量-效应、时间窗效应）
   - 管线二：R1–R40（安慰效应边界、数据不相容、影像学分层、资源分配）
   - 跨管线：R14–R40（自洽性、外泌体、资源分配、核心叙事）
5. 阶段 4：证据分级与跨层一致性检验
   - 管线一：GRADE 低，可达概率 15–25%
   - 管线二：GRADE 中低，可达概率 25–35%
   - 管线三：GRADE —，科学自洽性最高
6. 阶段 5：可证伪条件与行动建议
   - 管线一：明确机制主张（旁分泌主导），补充超选 IA 体内药代数据
   - 管线二：IIT 设计必须含假手术对照 + 影像学分层 + 双终点 + 年龄分层
   - 管线三：确定效应 miRNA（效力表征）
7. 阶段 6：多版本交付
   - 完整演化版：42,414 字，128 条参考文献
   - 精简结论版：33,837 字，115 条参考文献
   - 资源分配建议：管线一 30% + 管线二 40% + 管线三 30%
```

---

## 附录：关键数据点速查

### 管线一（IA 给药 NSC × 亚急性卒中）

| 数据点 | 数值 | 来源 | 证据等级 |
|---|---|---|---|
| IA 入脑率 | 0.6–0.9%，40–47% 归肝脾 | [1] | 全文 |
| 动物典型剂量 | 1 × 10⁶ | [1] | 全文 |
| 安全剂量（大鼠） | 0.25 × 10⁶ | [2] | 全文 |
| Fukuda 高剂量死亡率 | 39%（低剂量 27%，对照 33%） | [1] | 全文 |
| Giordano 无症状缺血征象 | 85%（6/7） | [1] | 全文 |
| 荟萃 mRS 0-2 @90d | RR 1.31 [1.01–1.70]，p=0.0436 | [3] | 全文 |
| 荟萃 mRS 0-1 @365d | RR 1.74 [1.09–2.77]，p=0.0198 | [3] | 全文 |
| 超选给药靶区暴露量 | 提高一个数量级 | [13] | 全文 |
| 可达概率 | **15–25%** | 综合 | — |

### 管线二（脑内移植 NSC × 慢性卒中）

| 数据点 | 数值 | 来源 | 证据等级 |
|---|---|---|---|
| NSI-566 FMMS 改善 | +16.0 分，p=0.0078，24 月稳定 | [4] | 全文 |
| ACTIsSIMA 应答率 | 12.7%/16.1% vs 假手术 13.5% | [5] | 注册库 |
| PISCES-2 主要终点 | 1/23 未达 | [8] | 全文 |
| 安慰效应幅度（rTMS） | FMA-UE 约 +2 分（66 分制） | [10] | 全文 |
| 慢性期时间窗效应 | mRS MD -0.60，NIHSS MD -3.20，均 P < 0.001 | [24] | 全文 |
| 运动训练效应量 | FMA WMD = 7.95 | [16] | 全文 |
| 慢性期自然恢复上限 | 3200–9600 次重复训练 8 周，无结构可塑性 | [29] | 全文 |
| 可达概率 | **25–35%** | 综合 | — |

### 跨管线

| 数据点 | 数值 | 来源 | 证据等级 |
|---|---|---|---|
| NPC 体内归巢率 | 0.3%，其中 <5% 呈神经元谱系 | [11] | 全文 |
| 体外 84% vs 体内神经元形成率 | **<0.015%** | 计算 | — |
| 外泌体旁分泌验证 | MSC 外泌体神经保护作用不依赖细胞替代 | [30] | 全文 |
| 资源分配（建议） | 管线一 30% + 管线二 40% + 管线三 30% | 综合 | — |

---

## 结语

这个 skill 固化的不是一个"模板"，而是一个**方法论**——如何**穷尽证据、对抗式审视、分级评估、可证伪输出**。它适用于任何需要**科学尽职调查**的场景：评估技术路线的可行性、判断科学主张的可信度、支持投资决策的证据基础。

核心价值：**让结论可以被检验，而不是永远正确。**
